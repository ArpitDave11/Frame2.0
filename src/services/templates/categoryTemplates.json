{
  "_meta": {
    "version": "7.0.0",
    "changelog": {
      "7.0.0": "Merged v6.0.0 + v2.1.0 + research. Restored Epic Status, totalWordTarget.excludes, max:0 for tables. Added ⭐ emphasis on critical sections. Crisp 30-50 word hints with imperative+example+anti-pattern formula. 10 templates.",
      "6.0.0": "Added general, ADR, lightweight_rfc. Goals & Non-Goals universal. diagramConfig on all. globalAntiPatterns.",
      "2.1.0": "Added Epic Status, excludes, specific formats. Inspired by Google/Amazon/Stripe."
    },
    "globalAntiPatterns": "Do not use filler ('It is important to note...'), buzzwords without definitions ('leverage', 'robust', 'seamless'), or restate the problem as the solution. Every claim must include a number, system name, or verifiable detail. Do not repeat prior sections — reference, don't restate.",
    "globalDefaults": {
      "statusEmoji": {
        "draft": "📝",
        "in_progress": "🔄",
        "review": "👀",
        "approved": "✅",
        "archived": "📦"
      },
      "reviewStates": {
        "approved": "✅",
        "pending": "⏳",
        "reviewing": "🔄",
        "changes_requested": "🟠",
        "rejected": "🔴"
      },
      "priorityLevels": {
        "critical": "🔴",
        "high": "🟠",
        "medium": "🟡",
        "low": "🟢"
      },
      "markdownFeatures": {
        "collapsibleSections": true,
        "mermaidDiagrams": true,
        "tables": true,
        "taskLists": true,
        "admonitions": false
      },
      "maxTitleLength": 100,
      "defaultPerPage": 20
    }
  },

  "general": {
    "description": "General-purpose specifications — AI classifies and structures content automatically",
    "tone": "Adaptive. Match formality and vocabulary to the detected domain.",
    "expertRole": "senior technical writer who adapts depth to the content domain",
    "storyStyle": "context-dependent",
    "architectureFocus": "determined by content classification",
    "totalWordTarget": { "min": 1500, "max": 4000, "excludes": ["tables", "diagrams"] },
    "diagramConfig": {
      "primary": { "type": "flowchart LR", "purpose": "System context showing main components and connections" },
      "secondary": { "type": "flowchart TD", "purpose": "Primary workflow with decision points and outcomes" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status (Draft/Review/Approved), Reviewers with approval emoji, Created/Updated dates, Confidence (High/Medium/Low)."
      },
      "Overview": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "Summarize the problem, affected users, and proposed approach in 3 paragraphs. Example: '1,200 monthly support tickets stem from manual data entry.' Do not write generic introductions."
      },
      "Goals & Non-Goals": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "List 3–5 measurable goals with target metrics. Then list 2–3 reasonable goals explicitly excluded. Example goal: 'Reduce manual entry errors from 37% to <5%.' Non-goals prevent scope creep."
      },
      "Requirements": {
        "target": 300, "max": 600, "format": "numbered-list",
        "hint": "List each requirement as: 'The system SHALL [verb] [object] [constraint].' Each must be independently testable. Example: 'The system SHALL process payments within 200ms at P99.' No vague 'should be fast.'"
      },
      "Scope & Non-Scope": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "List 3–5 in-scope items and 3–5 explicitly excluded items with reasoning. Non-scope is equally important as scope."
      },
      "Success Metrics": {
        "target": 150, "max": 300, "format": "slo-table",
        "columns": ["Metric", "Baseline", "Target", "Measurement Method", "Owner"],
        "hint": "Define 3–5 KPIs with current baseline, target value, and measurement method. Every metric must be quantified. 'Improve performance' is not a metric."
      },
      "Assumptions & Constraints": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "List each assumption with its validation status. List each constraint with its source (regulatory, technical, budget). Flag unvalidated assumptions."
      },
      "User Stories": {
        "target": 400, "max": 800, "format": "mixed",
        "hint": "Write as 'As a [specific role], I want [concrete capability] so that [measurable benefit].' Include 3–5 acceptance criteria per story. Not 'As a user' — name the actual role."
      }
    },
    "optionalSections": {
      "Architecture Overview": {
        "target": 200, "max": 0, "format": "mermaid", "diagram": "flowchart",
        "hint": "Draw a system context diagram showing primary components and data flows. Label every arrow with protocol/format."
      },
      "Risks": {
        "target": 150, "max": 300, "format": "risk-heat-map",
        "hint": "List top 5 risks ranked by probability × impact. Each must name a specific failure scenario AND a mitigation. Example: 'Vendor API deprecation (P:Med, I:High) → adapter pattern with v2 fallback.'"
      },
      "Dependencies": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "List each dependency with owning team, expected delivery date, and fallback plan if delayed."
      },
      "Timeline": {
        "target": 100, "max": 200, "format": "phase-table",
        "columns": ["Phase", "Duration", "Deliverables", "Exit Criteria"],
        "hint": "Define 3–5 phases with concrete dates and exit criteria. No phase without a deliverable."
      }
    }
  },

  "technical_design": {
    "description": "Technical design documents — architecture decisions, trade-offs, and implementation plans following Google Design Doc philosophy",
    "tone": "Precise, trade-off-focused, decision-forward. Define acronyms on first use.",
    "expertRole": "senior software architect with distributed systems expertise — write for tech leads and senior engineers",
    "storyStyle": "engineering-focused with clear acceptance criteria",
    "architectureFocus": "system architecture and component design",
    "totalWordTarget": { "min": 2000, "max": 5000, "excludes": ["tables", "code-blocks", "diagrams"] },
    "diagramConfig": {
      "primary": { "type": "flowchart LR", "purpose": "Component architecture showing services, databases, and data flows" },
      "secondary": { "type": "sequenceDiagram", "purpose": "Key interaction flow between the most important components" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers with approval emoji (✅/⏳/🔄), Created/Updated dates. Use Stripe gavel-block style — each reviewer gets a row."
      },
      "Objective": {
        "target": 150, "max": 300, "format": "prose",
        "hint": "State the technical problem and measurable outcome in 2–3 sentences. Example: 'Reduce API latency from 800ms to <200ms P99 by replacing synchronous DB calls with cached reads.' Not a restatement of the title."
      },
      "Context & Motivation": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "What triggered this design? Quantify the current pain. Example: '4,200 timeout errors/day traced to synchronous inventory checks during peak load.' Include system version, traffic patterns, constraints."
      },
      "Goals & Non-Goals": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "⭐ NON-GOALS are the most important part. List things this design intentionally does NOT solve, even if reasonable. Example non-goal: 'Multi-region failover is out of scope for Phase 1.' Prevents re-litigation."
      },
      "Architecture Overview": {
        "target": 400, "max": 800, "format": "prose", "diagram": "flowchart",
        "hint": "Describe the architecture with named components, their responsibilities, and data flow. Include a Mermaid diagram. Label every connection with protocol and data format."
      },
      "Technical Requirements": {
        "target": 300, "max": 600, "format": "numbered-list",
        "hint": "Each requirement: 'The system SHALL [verb] [object] [measurable constraint].' Example: 'The system SHALL handle 10K concurrent WebSocket connections with <50ms message delivery.' No vague 'scalable.'"
      },
      "Proposed Design": {
        "target": 500, "max": 1500, "format": "prose",
        "hint": "Describe HOW the solution works mechanically. Name technologies, data flows, and state changes. The design must describe a DIFFERENT mechanism than the problem. Do not restate goals as the design."
      },
      "Alternatives Considered": {
        "target": 200, "max": 400, "format": "comparison-table-and-prose",
        "columns": ["Option", "Pros", "Cons", "Effort", "Risk"],
        "hint": "⭐ MOST IMPORTANT SECTION (per Google). List 2–3 alternatives with specific pros/cons/effort. Example: 'DynamoDB rejected: eventual consistency unacceptable for ACID financial transactions.' Do not dismiss options without technical rationale."
      },
      "Cross-Cutting Concerns": {
        "target": 200, "max": 400, "format": "table",
        "columns": ["Concern", "Approach", "Impact"],
        "hint": "Address security, observability, performance, accessibility, and backward compatibility. Each must name the specific approach, not just acknowledge the concern."
      },
      "Implementation Plan": {
        "target": 300, "max": 600, "format": "phase-table",
        "columns": ["Phase", "Duration", "Deliverables", "Dependencies", "Exit Criteria"],
        "hint": "Break into 3–5 shippable increments. Each phase must have an exit criterion that proves it works. No phase without a deliverable."
      },
      "User Stories": {
        "target": 400, "max": 800, "format": "mixed",
        "hint": "Write engineering-focused stories with acceptance criteria. Example: 'As a platform engineer, I want circuit breaker on payment service so that cascading failures are contained within 5s.'"
      }
    },
    "optionalSections": {
      "Data Model": {
        "target": 250, "max": 500, "format": "schema-table",
        "columns": ["Field", "Type", "Required", "Description", "Constraints"],
        "hint": "Define every entity with field types, constraints, and relationships. Include indexes and migration strategy."
      },
      "API Design": {
        "target": 300, "max": 600, "format": "endpoint-blocks",
        "hint": "Document each endpoint: method, path, auth, request/response body with types, status codes, error cases."
      },
      "Sequence Diagrams": {
        "target": 150, "max": 0, "format": "mermaid-sequence", "diagram": "sequence",
        "hint": "Draw sequence diagrams for the 2–3 most complex interactions. Show error paths, not just happy path."
      },
      "Risks & Mitigations": {
        "target": 200, "max": 400, "format": "risk-heat-map",
        "hint": "Top 5 risks with specific failure scenarios and mitigation strategies. Each risk must name what breaks and how you recover."
      },
      "Open Questions": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "List unresolved decisions with owner, options considered, and decision deadline. Use task list format: - [ ] Question (@owner, due date)."
      },
      "Security & Non-Functional Requirements": {
        "target": 200, "max": 400, "format": "table",
        "columns": ["Category", "Requirement", "Target", "Measurement Method"],
        "hint": "Define performance, security, scalability targets with measurable thresholds. 'P99 latency <200ms' not 'should be fast.'"
      },
      "Testing Strategy": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "Define test types, coverage targets, and specific test scenarios for critical paths."
      }
    }
  },

  "business_requirement": {
    "description": "Business requirement documents — objectives, KPIs, RACI, and measurable outcomes for stakeholders",
    "tone": "Executive-friendly, outcome-focused. Lead with business impact and quantified metrics; minimize jargon.",
    "expertRole": "senior business analyst with enterprise process optimization expertise — write for executives and product managers",
    "storyStyle": "business-value focused with measurable outcomes",
    "architectureFocus": "business process and organizational impact",
    "totalWordTarget": { "min": 1500, "max": 3500, "excludes": ["tables", "appendices"] },
    "diagramConfig": {
      "primary": { "type": "flowchart TD", "purpose": "Business process flow with decision points, approval gates, and outcomes" },
      "secondary": { "type": "flowchart LR", "purpose": "System context showing business systems involved" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers with approval emoji, Created/Updated dates, Business Sponsor."
      },
      "Executive Summary": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "Summarize the business impact, cost/benefit, and recommendation in 3 paragraphs. A VP should be able to approve or reject from this section alone."
      },
      "Goals & Non-Goals": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "SMART goals: Specific, Measurable, Achievable, Relevant, Time-bound. Example: 'Reduce churn by 15% within 6 months.' List 2–3 non-goals to prevent scope expansion."
      },
      "Business Context & Problem Statement": {
        "target": 250, "max": 500, "format": "prose",
        "hint": "Lead with data. Quantify the pain: affected users, error rates, dollar costs. Example: '37% of support tickets (1,200/month) stem from manual data entry, costing ~$180K/quarter.' No opinions without evidence."
      },
      "Scope & Non-Scope": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "List in-scope deliverables and explicitly excluded items with reasoning. Non-scope prevents scope creep."
      },
      "Stakeholders & RACI": {
        "target": 0, "max": 0, "format": "raci-table",
        "columns": ["Deliverable", "Responsible", "Accountable", "Consulted", "Informed"],
        "hint": "Each row must have exactly ONE Accountable person. Name specific roles/people, not teams. Every deliverable from Scope must appear here."
      },
      "Requirements": {
        "target": 300, "max": 600, "format": "numbered-list",
        "hint": "Prioritized business requirements with unique IDs (BR-001). Each gets 30–80 words max. Use MoSCoW priority: Must/Should/Could/Won't."
      },
      "Process Flow": {
        "target": 200, "max": 0, "format": "mermaid", "diagram": "flowchart",
        "hint": "Draw the proposed business process with decision points, approval gates, and exception paths. Label every arrow with the trigger condition."
      },
      "Success Metrics": {
        "target": 150, "max": 300, "format": "slo-table",
        "columns": ["Metric", "Baseline", "Target", "Measurement Method", "Review Cadence"],
        "hint": "Every metric must be quantified. 'Improve performance' is not a metric. 'Reduce ticket volume from 1,200/mo to <300/mo' is."
      },
      "User Stories": {
        "target": 300, "max": 600, "format": "mixed",
        "hint": "Business-value stories with quantified benefits. Example: 'As a finance manager, I want automated reconciliation so that month-end close reduces from 5 days to 1 day.'"
      }
    },
    "optionalSections": {
      "Current State (As-Is)": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "Describe the current process with specific pain points, workarounds, and costs. Include a process diagram if complex."
      },
      "Risk Assessment": {
        "target": 200, "max": 400, "format": "risk-heat-map",
        "hint": "Top 5 business risks with probability × impact. Each must name the specific failure and mitigation."
      },
      "Cost-Benefit Analysis": {
        "target": 200, "max": 400, "format": "table",
        "columns": ["Item", "One-Time Cost", "Annual Cost", "Annual Benefit", "Payback Period"],
        "hint": "Quantify all costs and benefits. Include development, licensing, training, support. Show ROI and payback period."
      },
      "Timeline & Milestones": {
        "target": 150, "max": 300, "format": "phase-table",
        "columns": ["Phase", "Duration", "Deliverables", "Business Milestone"],
        "hint": "Define phases with business milestones, not just technical deliverables."
      },
      "Next Steps & Decision Log": {
        "target": 80, "max": 150, "format": "bullet-list",
        "hint": "Use task lists (- [ ]) for action items. Decision log: Decision, Date, Rationale, Participants."
      }
    }
  },

  "feature_specification": {
    "description": "Feature specifications — user-centric design with evidence-based problem statements, edge cases, and launch plans",
    "tone": "Clear, user-centric, evidence-driven. Describe behavior from the user's perspective; name specific screens and actions.",
    "expertRole": "senior product manager with UX and data-driven prioritization — write for designers, developers, and QA",
    "storyStyle": "user-centric with detailed acceptance criteria",
    "architectureFocus": "feature behavior and user experience",
    "totalWordTarget": { "min": 1500, "max": 3500, "excludes": ["tables", "diagrams"] },
    "diagramConfig": {
      "primary": { "type": "flowchart TD", "purpose": "User journey showing screens, actions, decisions, and error states" },
      "secondary": { "type": "stateDiagram-v2", "purpose": "Feature state transitions (e.g., draft → submitted → approved)" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers, Created/Updated dates, Target Release."
      },
      "Problem Statement": {
        "target": 150, "max": 300, "format": "prose",
        "hint": "Lead with evidence — user research data, support ticket counts, analytics. Example: '42% of users abandon checkout at the address step (Hotjar session data, n=3,200).' No opinions without data."
      },
      "Goals & Non-Goals": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "Measurable feature goals with target metrics. Non-goals prevent feature creep. Example non-goal: 'Mobile-native experience is Phase 2; responsive web is sufficient for launch.'"
      },
      "User Personas": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "Define 2–3 personas with role, technical proficiency, primary goal, and pain point. Not generic 'users' — name the archetype."
      },
      "Functional Requirements": {
        "target": 300, "max": 600, "format": "numbered-list",
        "hint": "Each requirement with unique ID (FR-001), priority (MoSCoW), and testable acceptance criterion. Example: 'FR-003 (Must): Search returns results within 500ms for queries up to 100 characters.'"
      },
      "Scope & Non-Scope": {
        "target": 100, "max": 250, "format": "bullet-list",
        "hint": "List features explicitly excluded from this release with reasoning. 'Not in v1' prevents scope creep."
      },
      "User Flows": {
        "target": 200, "max": 0, "format": "mermaid", "diagram": "flowchart",
        "hint": "Include Mermaid flowcharts for happy path AND key error paths. Show every decision point and error state. Label transitions with user actions."
      },
      "Edge Cases & Error Handling": {
        "target": 200, "max": 400, "format": "table",
        "columns": ["Scenario", "Trigger", "Expected Behavior", "Priority"],
        "hint": "List edge cases with specific trigger conditions and expected behavior. Include: empty states, boundary values, concurrent access, network failures."
      },
      "Non-Functional Requirements": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Category", "Requirement", "Target", "Measurement"],
        "hint": "Be specific: 'P99 latency <200ms' not 'should be fast.' Include performance, accessibility (WCAG 2.1 AA), and i18n requirements."
      },
      "Analytics & Success Metrics": {
        "target": 100, "max": 200, "format": "slo-table",
        "columns": ["Event", "Trigger", "Properties", "Success Target"],
        "hint": "Define every trackable event with trigger, properties captured, and success threshold. Example: 'checkout_complete: conversion rate >68%.'"
      },
      "User Stories": {
        "target": 400, "max": 800, "format": "mixed",
        "hint": "User-centric stories with specific screen names, actions, and acceptance criteria. Each story must begin with a bold, scannable title."
      }
    },
    "optionalSections": {
      "Launch Plan": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "Feature flag strategy, rollout %, rollback criteria. Example: 'Launch to 5% → monitor error rate 24h → expand to 25% → full rollout.'"
      },
      "UX/Design": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "Link to Figma/mockups. Describe key interaction patterns and accessibility considerations."
      },
      "Dependencies & Risks": {
        "target": 0, "max": 0, "format": "risk-heat-map",
        "hint": "List dependencies with teams/dates. Top 3 risks with failure scenario and mitigation."
      },
      "Competitive Analysis": {
        "target": 100, "max": 200, "format": "comparison-table-and-prose",
        "columns": ["Competitor", "Approach", "Strength", "Gap"],
        "hint": "How do competitors solve this? What can we learn? What differentiates our approach?"
      }
    }
  },

  "api_specification": {
    "description": "API specifications — endpoints, schemas, auth, rate limits, and versioning modeled after Stripe's gold standard",
    "tone": "Developer-friendly, example-driven, precise. Every endpoint must be copy-paste implementable from the doc alone.",
    "expertRole": "senior API architect with RESTful and event-driven design — write for API consumers and integration engineers",
    "storyStyle": "API-consumer focused with contract clarity",
    "architectureFocus": "API contract and integration patterns",
    "totalWordTarget": { "min": 2000, "max": 5000, "excludes": ["code-blocks", "tables", "schemas"] },
    "diagramConfig": {
      "primary": { "type": "sequenceDiagram", "purpose": "Primary API request/response flow including auth, validation, and error paths" },
      "secondary": { "type": "flowchart LR", "purpose": "API topology showing gateway, services, and data stores" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers, API Version, Base URL, Created/Updated dates."
      },
      "Objective": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "What does this API do, who consumes it, and what's the key design constraint? One paragraph."
      },
      "Overview & Authentication": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "Base URLs per environment, auth mechanism (OAuth2/API Key/JWT), token lifecycle, scopes. Include a curl example for the auth flow."
      },
      "Goals & Non-Goals": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "API design goals (backward compatibility, idempotency, etc.) and explicit exclusions. Example non-goal: 'GraphQL interface is out of scope for v1.'"
      },
      "Endpoints": {
        "target": 500, "max": 1500, "format": "endpoint-blocks",
        "hint": "⭐ Working examples are MANDATORY. Each endpoint: method, path, description, auth, request body, response body, status codes, error cases. Use code blocks for request/response."
      },
      "Data Models": {
        "target": 0, "max": 0, "format": "schema-table",
        "columns": ["Field", "Type", "Required", "Description", "Constraints"],
        "hint": "One table per resource. Include validation rules and examples for each field. Flag breaking changes with inline diff."
      },
      "Error Handling": {
        "target": 0, "max": 0, "format": "error-table",
        "columns": ["HTTP Status", "Error Code", "Description", "Recovery Action", "Retry Strategy"],
        "hint": "Standardized error envelope with request_id for traceability. Every error must tell the consumer how to fix it."
      },
      "Pagination & Rate Limiting": {
        "target": 100, "max": 200, "format": "table",
        "columns": ["Limit Type", "Value", "Scope", "Header"],
        "hint": "Exact numbers: requests/minute, burst limit, pagination strategy (cursor vs offset), page size defaults and max."
      },
      "User Stories": {
        "target": 300, "max": 600, "format": "mixed",
        "hint": "API consumer stories specifying integration patterns. Example: 'As a billing service, I want idempotent charge creation so that network retries don't duplicate payments.'"
      }
    },
    "optionalSections": {
      "Webhooks & Events": {
        "target": 150, "max": 300, "format": "endpoint-blocks",
        "hint": "Include payload examples. Specify delivery guarantee (at-least-once), retry policy, and signature verification."
      },
      "Versioning & Deprecation": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "URL vs header versioning, backward compatibility guarantees, deprecation timeline, sunset policy."
      },
      "SDK Examples": {
        "target": 200, "max": 400, "format": "code-block",
        "hint": "Working code in 2–3 languages (Python, Node.js, cURL). Each example must be copy-paste runnable."
      },
      "Testing & Sandbox": {
        "target": 80, "max": 150, "format": "prose",
        "hint": "Sandbox URLs, test credentials, mock data, Postman collection link."
      },
      "Idempotency": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "Which endpoints support idempotent requests, key format, TTL, and conflict resolution strategy."
      },
      "Migration Guide": {
        "target": 150, "max": 300, "format": "numbered-procedure",
        "hint": "Step-by-step guide for migrating from previous API version with code diff examples."
      }
    }
  },

  "infrastructure_design": {
    "description": "Infrastructure design — cloud architecture, SLOs, DR, cost analysis. Every component must answer: how does it fail, how do we know, how do we recover?",
    "tone": "Ops-focused, SLO-driven, cost-aware. Specify exact resource sizes, regions, and configurations.",
    "expertRole": "senior infrastructure architect with cloud-native and SRE expertise — write for platform engineers and DevOps",
    "storyStyle": "operations-focused with SLO targets",
    "architectureFocus": "infrastructure topology and operations",
    "totalWordTarget": { "min": 2000, "max": 5000, "excludes": ["tables", "diagrams", "runbooks"] },
    "diagramConfig": {
      "primary": { "type": "flowchart LR", "purpose": "Infrastructure layout showing compute, storage, networking, and external services" },
      "secondary": { "type": "flowchart TD", "purpose": "CI/CD pipeline stages from commit to production" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers, Cloud Provider, Region, Created/Updated dates."
      },
      "Objective": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "What infrastructure, why, and the key SLO target. Example: 'Provision multi-AZ payment processing cluster targeting 99.99% availability.'"
      },
      "Goals & Non-Goals": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "Infrastructure goals with SLO targets. Non-goals: what this infra intentionally does NOT provide. Example non-goal: 'Multi-region DR is Phase 2.'"
      },
      "SLA/SLO Requirements": {
        "target": 0, "max": 0, "format": "slo-table",
        "columns": ["Service", "SLI", "SLO Target", "Window", "Error Budget", "Alert Threshold"],
        "hint": "Define availability, latency percentiles, throughput, and error rate targets per service. Include error budget calculation."
      },
      "Architecture Diagram": {
        "target": 300, "max": 0, "format": "mermaid", "diagram": "flowchart",
        "hint": "⭐ MANDATORY. Complete infrastructure topology: compute, storage, networking, external services. Show AZs, VPCs, load balancers. Label every connection with protocol."
      },
      "Compute & Storage": {
        "target": 250, "max": 500, "format": "table",
        "columns": ["Component", "Service", "Size/Type", "Count", "Monthly Cost", "Scaling Policy"],
        "hint": "Specify every resource with exact types, sizes, and counts. Include scaling policy and cost. No hand-waving about 'appropriate sizing.'"
      },
      "Networking & Security": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "VPC design, subnets, security groups, NACLs, TLS config, certificate management, WAF rules. Name specific CIDR ranges."
      },
      "CI/CD Pipeline": {
        "target": 200, "max": 0, "format": "mermaid", "diagram": "flowchart",
        "hint": "Draw the full pipeline: commit → build → test → stage → canary → production. Include rollback triggers at each stage."
      },
      "Monitoring & Alerting": {
        "target": 200, "max": 400, "format": "slo-table",
        "columns": ["Service", "SLI", "SLO Target", "Window", "Error Budget", "Alert Threshold"],
        "hint": "Define SLOs for every service with alert thresholds and escalation paths. Include dashboard links."
      },
      "Disaster Recovery": {
        "target": 200, "max": 400, "format": "numbered-procedure",
        "hint": "⭐ CRITICAL. Define RTO and RPO per service tier. Step-by-step recovery procedure with time estimates per step. Include: when was this last tested?"
      },
      "Cost Analysis": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Component", "Monthly Cost", "Annual Cost", "Optimization Opportunity"],
        "hint": "Current vs projected spend. Flag top-3 cost drivers. Include reserved instance vs on-demand comparison."
      },
      "User Stories": {
        "target": 300, "max": 600, "format": "mixed",
        "hint": "Operations-focused stories with SLO targets. Example: 'As an SRE, I want automated failover so that RTO <5min when a primary AZ fails.'"
      }
    },
    "optionalSections": {
      "Capacity Planning": {
        "target": 150, "max": 300, "format": "table",
        "columns": ["Resource", "Current Usage", "6-Month Projection", "12-Month Projection", "Action Required"],
        "hint": "Project growth for compute, storage, and network over 6 and 12 months with trigger points for scaling."
      },
      "Scaling Strategy": {
        "target": 150, "max": 300, "format": "prose",
        "hint": "Horizontal vs vertical. Auto-scaling policies with triggers, cooldowns, min/max. Include cost implications."
      },
      "Operational Runbooks": {
        "target": 0, "max": 0, "format": "bullet-list",
        "hint": "Index of runbooks with links. Common incident procedures, escalation paths, and known-issue workarounds."
      }
    }
  },

  "migration_plan": {
    "description": "Migration plans as operational playbooks — current/target architectures, RACI, risk heat maps, cutover procedures, rollback plans",
    "tone": "Procedural, risk-aware, operationally precise. Every step must have a validation checkpoint and rollback trigger.",
    "expertRole": "senior migration architect with enterprise data migration expertise — write for DBAs, ops, and project managers",
    "storyStyle": "migration-step focused with rollback criteria",
    "architectureFocus": "migration process and data integrity",
    "totalWordTarget": { "min": 2000, "max": 4000, "excludes": ["tables", "diagrams", "checklists"] },
    "diagramConfig": {
      "primary": { "type": "flowchart TD", "purpose": "Migration process with validation gates, rollback points, and go/no-go decisions" },
      "secondary": { "type": "flowchart LR", "purpose": "Current state (left) to target state (right) architecture transition" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers, Migration Window, Rollback Deadline, Created/Updated."
      },
      "Objective": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "What's migrating, from where to where, when, and the zero-data-loss commitment. One paragraph."
      },
      "Goals & Non-Goals": {
        "target": 200, "max": 400, "format": "bullet-list",
        "hint": "Measurable migration goals. Non-goals: what this migration intentionally skips. Example non-goal: 'Schema refactoring is out of scope — lift-and-shift only.'"
      },
      "Current State": {
        "target": 200, "max": 400, "format": "prose", "diagram": "flowchart",
        "hint": "Architecture diagram of current system. Include: version, data volume, known tech debt, dependencies. Mermaid diagram mandatory."
      },
      "Target State": {
        "target": 200, "max": 400, "format": "prose", "diagram": "flowchart",
        "hint": "Architecture diagram of target system. Highlight what changes vs what stays. Mermaid diagram mandatory."
      },
      "Migration Strategy": {
        "target": 100, "max": 300, "format": "prose",
        "hint": "Big-bang vs phased vs trickle. Lift-and-shift vs re-platform vs re-architect. Justify the choice with risk/effort trade-off."
      },
      "Scope & Non-Scope": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "What's included in THIS migration phase? What's explicitly excluded? What's deferred to Phase 2?"
      },
      "Data Mapping": {
        "target": 300, "max": 0, "format": "mapping-table",
        "columns": ["Source Field", "Source Type", "Target Field", "Target Type", "Transformation", "Validation"],
        "hint": "Map every source field to target. Include transformation rules and validation criteria. No field left unmapped."
      },
      "RACI Matrix": {
        "target": 0, "max": 0, "format": "raci-table",
        "columns": ["Task", "Responsible", "Accountable", "Consulted", "Informed"],
        "hint": "Migrations cross multiple teams. RACI eliminates ambiguity. One Accountable per task. Name people, not teams."
      },
      "Risk Assessment": {
        "target": 0, "max": 0, "format": "risk-heat-map",
        "hint": "⭐ HIGHEST-STAKES DOCUMENT TYPE. Risk assessment must be exhaustive. Include: data loss, downtime, rollback failure, performance degradation, compliance violation scenarios."
      },
      "Cutover Plan": {
        "target": 200, "max": 600, "format": "numbered-procedure",
        "hint": "Step-by-step numbered procedure. Include time estimates per step, validation checkpoints, go/no-go criteria at each gate. Example: 'Step 4 (15min): Verify record count matches ±0.01%.'"
      },
      "Rollback Plan": {
        "target": 150, "max": 400, "format": "numbered-procedure",
        "hint": "⭐ CRITICAL. Explicit triggers: what conditions activate rollback? Steps with time estimates. Data recovery procedure. Example trigger: 'If error rate >1% within 30min of cutover.'"
      },
      "Validation Criteria": {
        "target": 150, "max": 300, "format": "numbered-list",
        "hint": "Go/no-go criteria with specific thresholds. Example: 'Record count match ±0.01%, checksum match 100%, P99 latency <500ms for 15min.'"
      },
      "User Stories": {
        "target": 300, "max": 600, "format": "mixed",
        "hint": "Migration-focused stories with rollback awareness. Example: 'As a DBA, I want point-in-time recovery so that if migration corrupts data, I can restore to pre-cutover state within RTO.'"
      }
    },
    "optionalSections": {
      "Timeline & Milestones": {
        "target": 0, "max": 0, "format": "phase-table",
        "columns": ["Milestone", "Date", "Owner", "Go/No-Go Criteria"],
        "hint": "Each milestone needs a go/no-go gate. Include dry run dates."
      },
      "Dry Run Plan": {
        "target": 100, "max": 200, "format": "numbered-procedure",
        "hint": "Pre-cutover rehearsal with success criteria, data subset, and timing comparison to production estimates."
      },
      "Testing Strategy": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "Define test types (unit, integration, smoke, perf), data subsets, and pass criteria per test."
      },
      "Communication Plan": {
        "target": 0, "max": 0, "format": "raci-table",
        "columns": ["Stakeholder", "When", "Channel", "Message", "Owner"],
        "hint": "Who needs to know what, when? Include customer-facing comms if applicable."
      },
      "Hypercare Plan": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "Post-migration monitoring period with staffing, escalation paths, and known-issue workarounds."
      },
      "Data Security & Compliance": {
        "target": 80, "max": 200, "format": "bullet-list",
        "hint": "Data classification, encryption in transit/at rest, audit logging, regulatory requirements."
      }
    }
  },

  "integration_spec": {
    "description": "Integration specifications as enforceable contracts — data mappings, sequence diagrams, retry policies, SLAs, and monitoring",
    "tone": "Contract-precise, failure-aware. Define happy path and every failure mode at each integration boundary.",
    "expertRole": "senior integration architect with enterprise middleware and event-driven expertise — write for integration developers and ops",
    "storyStyle": "integration-point focused with contract clarity",
    "architectureFocus": "system integration and data exchange",
    "totalWordTarget": { "min": 1800, "max": 4000, "excludes": ["tables", "diagrams", "schemas"] },
    "diagramConfig": {
      "primary": { "type": "flowchart LR", "purpose": "System landscape showing all integration points and data flows between systems" },
      "secondary": { "type": "sequenceDiagram", "purpose": "Primary data exchange flow between source and target systems" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "Governance table: Owner, Status, Reviewers, Systems Involved, Created/Updated dates."
      },
      "Objective": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "What systems are integrated, what data flows, and the key reliability requirement. One paragraph."
      },
      "Goals & Non-Goals": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "Integration goals with reliability targets. Non-goals: excluded capabilities. Example non-goal: 'Bi-directional sync is Phase 2 — this phase is source→target only.'"
      },
      "Integration Overview": {
        "target": 200, "max": 0, "format": "mermaid", "diagram": "flowchart",
        "hint": "System context diagram showing all participating systems, middleware, and data flows. Label every connection with protocol and direction."
      },
      "Systems & Endpoints": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["System", "Role", "Protocol", "Base URL", "Auth Method", "Owner"],
        "hint": "One row per system. Include environment-specific URLs (dev/staging/prod)."
      },
      "Data Contract & Mappings": {
        "target": 0, "max": 0, "format": "mapping-table",
        "columns": ["Source Field", "Source Type", "Target Field", "Target Type", "Transformation", "Validation"],
        "hint": "Include schema validation rules. Use inline diff for version changes. Every field mapped — no field left undefined."
      },
      "Integration Flows": {
        "target": 200, "max": 0, "format": "mermaid-sequence", "diagram": "sequence",
        "hint": "⭐ MUST include both happy path AND error paths. Draw sequence diagrams showing request/response, timeouts, retries, and circuit breaker activation."
      },
      "Error Handling & Recovery": {
        "target": 200, "max": 400, "format": "error-table",
        "columns": ["Error Scenario", "Source System", "Detection", "Recovery Action", "Escalation"],
        "hint": "Retry policies: max attempts, backoff strategy (exponential with jitter), circuit breaker thresholds. Dead-letter queue handling."
      },
      "Monitoring & Alerting": {
        "target": 0, "max": 0, "format": "slo-table",
        "columns": ["Metric", "SLO Target", "Alert Threshold", "Dashboard", "Escalation"],
        "hint": "Monitor integration health, not just system health. Track: message lag, error rate per integration point, schema validation failures."
      },
      "Performance & SLAs": {
        "target": 0, "max": 0, "format": "slo-table",
        "columns": ["Integration Point", "SLA", "Latency Target", "Throughput", "Breach Escalation"],
        "hint": "SLA per integration point with breach escalation paths. Include: max message size, throughput limits, batch size constraints."
      },
      "User Stories": {
        "target": 300, "max": 600, "format": "mixed",
        "hint": "Integration-focused stories specifying systems and data flows. Example: 'As the billing system, I want order events within 5s of creation so that invoices generate in real-time.'"
      }
    },
    "optionalSections": {
      "Security & Data Privacy": {
        "target": 150, "max": 300, "format": "bullet-list",
        "hint": "Auth mechanism per integration point, encryption (TLS version), PII handling, data retention, audit logging."
      },
      "Versioning & Compatibility": {
        "target": 80, "max": 150, "format": "prose",
        "hint": "How are breaking changes handled across system boundaries? Contract versioning strategy."
      },
      "Fallback & Degradation": {
        "target": 80, "max": 150, "format": "prose",
        "hint": "What happens when the integration is down? Graceful degradation strategy with cached/stale data handling."
      },
      "Testing Strategy": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "Contract tests, integration tests, chaos testing for failure scenarios. Include test data strategy."
      },
      "Comparison of Approaches": {
        "target": 200, "max": 400, "format": "comparison-table-and-prose",
        "columns": ["Pattern", "Pros", "Cons", "Effort", "Risk"],
        "hint": "Compare 2–3 integration patterns (REST vs events vs batch) and explain the choice."
      }
    }
  },

  "architecture_decision_record": {
    "description": "ADR — captures WHY a decision was made, not just what. Immutable once accepted. Sequentially numbered.",
    "tone": "Concise, factual, decision-focused. Present tense for active decisions, past tense for superseded.",
    "expertRole": "senior architect documenting a decision for future engineers who will ask 'why was this done this way?'",
    "storyStyle": "decision-outcome focused with explicit trade-off acknowledgment",
    "architectureFocus": "single architectural decision and its ripple effects",
    "totalWordTarget": { "min": 500, "max": 1500, "excludes": ["tables"] },
    "diagramConfig": {
      "primary": { "type": "flowchart TD", "purpose": "Decision tree or component affected by this decision" },
      "secondary": { "type": "flowchart LR", "purpose": "System context showing components affected" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "ADR header: ADR Number, Status (Proposed/Accepted/Deprecated/Superseded), Date, Deciders."
      },
      "Context": {
        "target": 150, "max": 300, "format": "prose",
        "hint": "Describe the technical and business forces at play. What makes this decision necessary NOW? Be factual, not persuasive."
      },
      "Decision Drivers": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "List the specific constraints forcing this decision: performance limits, compliance requirements, team capacity, timeline."
      },
      "Goals & Non-Goals": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "2–3 goals this decision achieves. 1–2 things explicitly NOT addressed. Keeps the decision focused."
      },
      "Considered Options": {
        "target": 200, "max": 400, "format": "comparison-table-and-prose",
        "columns": ["Option", "Pros", "Cons", "Effort", "Risk"],
        "hint": "⭐ CORE OF AN ADR. List 2–4 options with honest pros/cons. A rubber-stamp ADR with one 'obvious' option is useless."
      },
      "Decision Outcome": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "State the chosen option and WHY — referencing specific decision drivers. Example: 'Chose PostgreSQL over DynamoDB because ACID transactions are non-negotiable for payment processing.'"
      },
      "Consequences": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "List positive AND negative consequences. Include: new constraints introduced, teams affected, tech debt accepted, future doors opened/closed."
      }
    },
    "optionalSections": {
      "Related Decisions": {
        "target": 50, "max": 100, "format": "bullet-list",
        "hint": "Link to ADRs this decision depends on or supersedes."
      },
      "Notes": {
        "target": 50, "max": 100, "format": "prose",
        "hint": "Additional context that doesn't fit elsewhere. Meeting notes, external references."
      }
    }
  },

  "lightweight_rfc": {
    "description": "Lightweight RFC / One-Pager — minimal proposal for small-scope changes that would otherwise go undocumented",
    "tone": "Direct and minimal. Every sentence must justify its existence. No preamble, no filler.",
    "expertRole": "pragmatic engineer proposing a focused change — write only what a reviewer needs to approve or reject",
    "storyStyle": "change-proposal focused with clear accept/reject criteria",
    "architectureFocus": "single component or process change",
    "totalWordTarget": { "min": 500, "max": 1500, "excludes": ["tables"] },
    "diagramConfig": {
      "primary": { "type": "flowchart LR", "purpose": "Before/after showing the component or process being changed" },
      "secondary": { "type": "flowchart TD", "purpose": "Impact scope showing affected components" }
    },
    "requiredSections": {
      "Epic Status": {
        "target": 0, "max": 0, "format": "table",
        "columns": ["Field", "Value"],
        "hint": "RFC header: Author, Status (Proposed/Accepted/Rejected), Decision Deadline, Approvers."
      },
      "Problem Statement": {
        "target": 100, "max": 200, "format": "prose",
        "hint": "State the specific problem in 2–3 sentences with quantified evidence. Example: 'Deploy failures increased 3× since adding the canary step (from 2% to 6% of deploys).'"
      },
      "Goals & Non-Goals": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "2–3 goals this RFC achieves. 1–2 things explicitly excluded. Keep scope ruthlessly small."
      },
      "Proposed Solution": {
        "target": 200, "max": 400, "format": "prose",
        "hint": "Describe HOW the solution works. Name the specific technology, config change, or code change. The solution must be a DIFFERENT mechanism than the problem."
      },
      "Impact & Scope": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "List affected systems, teams, and rollback strategy. Example: 'Affects: deploy pipeline. Rollback: revert config, re-run pipeline. Teams: platform.'"
      }
    },
    "optionalSections": {
      "Alternatives": {
        "target": 100, "max": 200, "format": "bullet-list",
        "hint": "1–2 alternatives considered and why rejected. Even for small changes, showing you considered options builds trust."
      },
      "Open Questions": {
        "target": 50, "max": 100, "format": "bullet-list",
        "hint": "Unresolved questions with owners and decision deadlines."
      }
    }
  }
}