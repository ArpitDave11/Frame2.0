apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "frame-spa.fullname" . }}
  labels:
    {{- include "frame-spa.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "frame-spa.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "frame-spa.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "frame-spa.fullname" . }}
      containers:
        - name: spa
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
