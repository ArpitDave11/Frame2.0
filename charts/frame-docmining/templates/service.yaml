apiVersion: v1
kind: Service
metadata:
  name: {{ include "frame-docmining.fullname" . }}
  labels:
    {{- include "frame-docmining.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frame-docmining.selectorLabels" . | nindent 4 }}
