# Copy this to `.env` (gitignored).
# Offline runtime: the service must never hit HuggingFace after first startup.

DOCMINING_ARTIFACTS_PATH=./models
HF_HUB_OFFLINE=1
TRANSFORMERS_OFFLINE=1
HF_HUB_DISABLE_TELEMETRY=1
TOKENIZERS_PARALLELISM=false
